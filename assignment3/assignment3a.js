console.log(`iterating using for loop`);
                var pen = [`montex`,`cellpoint`,`renolds`,`cello pinpoint`,`marker`];
                for(var i=0;i<pen.length;i++){
                    console.log(`pen =`,pen[i]);
                }
                console.log(`---------------------------------------`);
                var pencil = [`apsara`,`natraj`,`charcoltype`,`camlin`,`leadtype`];
                for(var i=0;i<pencil.length;i++){
                    console.log(`pencil =`,pencil[i]);
                }
                console.log(`---------------------------------------`);
                var coolDrinks = [`thumbsup`,`sprite`,`limca`,`sevenup`,`marinda`];
                for(var i=0;i<coolDrinks.length;i++){
                    console.log(`cooldrinks =`,coolDrinks[i]);
                }
                console.log(`---------------------------------------`);
                var beer = [`kingfisher`,`magnum`,`blenderspride`,`bedwieser`,`calsberg`];
                for(var i=0;i<beer.length;i++){
                    console.log(`beer =`,beer[i]);
                }
                console.log(`---------------------------------------`);
                var milkPowder = [`boost`,`horlicks`,`complain`,`bornvita`,`ensure`];
                for(var i=0;i<milkPowder.length;i++){
                    console.log(`milkpowder =`,milkPowder[i]);
                }
                console.log(`---------------------------------------`);
                var food = [`idli`,`vada`,`dosa`,`masala dosa`,`onion dosa`];
                for(var i=0;i<food.length;i++){
                    console.log(`food =`,food[i]);
                }
               
                console.log(`---------------------------------------`);
                var cars = [`maruthi`,`audi`,`fortuner`,`mahindra`,`benz`];
                for(var i=0;i<cars.length;i++){
                    console.log(`car =`,cars[i]);
                }
                console.log(`---------------------------------------`);
                var bikes = [`royal enfield`,`pulser`,`fz`,`rx100`,`r15`];
                for(var i=0;i<bikes.length;i++){
                    console.log(`bike =`,bikes[i]);
                }
                console.log(`---------------------------------------`);
                var toothPaste = [`colgate`,`closeup`,`dubber red`,`pepsodent`,`pathanjali`];
                for(var i=0;i<toothPaste.length;i++){
                    console.log(`toothpaste =`,toothPaste[i]);
                }
                console.log(`---------------------------------------`);
                var soaps = [`santoor`,`lux`,`dettol`,`life boy`,`no 1`];
                for(var i=0;i<soaps.length;i++){
                    console.log(`soap =`,soaps[i]);
                }
                console.log(`========================================`);